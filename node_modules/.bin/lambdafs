#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../chrome-aws-lambda/node_modules/lambdafs/bin/brotli.js" "$@"
  ret=$?
else 
  node  "$basedir/../chrome-aws-lambda/node_modules/lambdafs/bin/brotli.js" "$@"
  ret=$?
fi
exit $ret
